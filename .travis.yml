language: python

python:
#  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
dist: xenial
sudo: true

matrix:
  fast_finish: true

install:
  - pip install tox
  - pip install coverage
  - pip install coveralls
  - pip install .

script:
# - tox
  - coverage run test.py

after_success:
  - coverage report
  - coveralls
